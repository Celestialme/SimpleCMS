
	
	import widgets from '../components/widgets';
	import type { Schema } from './types';
	let schema: Schema = {
		name: 'as',
		fields: [
			widgets.Email(
					{"label":"as"}
				)
		]
	};
	export default schema;
	
	