let schema = {
    icon: 'lets-icons:menu',
    fields: [
        globalThis.widgets.Relation({
            label: 'relation',
            relation: 'Posts3',
            displayPath: 'text 1'
        })
    ]
};
export default schema;
