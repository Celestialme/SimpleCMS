let schema = {
    icon: 'iconoir:post',
    fields: [
        globalThis.widgets.Text({
            label: 'text',
            translated: true
        }),
        globalThis.widgets.Text({
            label: 'text2'
        })
    ]
};
export default schema;
