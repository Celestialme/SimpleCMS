[
	{
		"display": "async (data, field2, entry, contentLanguage) => {\n      data = data ? data : {};\n      return translated ? data[contentLanguage] || \"NO entry\" : data[PUBLIC_CONTENT_LANGUAGE] || \"NO entry\";\n    }",
		"label": "text",
		"translated": false,
		"widget": "Text"
	},
	{
		"display": "async (data, field2, entry, contentLanguage) => {\n      data = data ? data : {};\n      return translated ? data[contentLanguage] || \"NO entry\" : data[PUBLIC_CONTENT_LANGUAGE] || \"NO entry\";\n    }",
		"label": "text2",
		"translated": false,
		"widget": "Text"
	},
	{
		"display": "async (data, field2, entry, contentLanguage) => {\n      data = data ? data : {};\n      return translated ? data[contentLanguage] || \"NO entry\" : data[PUBLIC_CONTENT_LANGUAGE] || \"NO entry\";\n    }",
		"label": "text3",
		"translated": false,
		"widget": "Text"
	}
]
